import { Box } from "@mui/material";

const BackOfficeSubCategories = (): React.ReactNode => {
  return (
    <Box>
      <h1>{`SubCategories! I'm so excited to see what you're going to build here!`}</h1>
    </Box>
  );
};

export default BackOfficeSubCategories;
