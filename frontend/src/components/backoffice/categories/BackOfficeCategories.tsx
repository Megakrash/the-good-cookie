import { Box } from "@mui/material";

const BackOfficeCategories = (): React.ReactNode => {
  return (
    <Box>
      <h1>{`Categories! I'm so excited to see what you're going to build here!`}</h1>
    </Box>
  );
};

export default BackOfficeCategories;
